# Kuikly 页面生成器 · 优化路线图

> 目标：不重复造编排轮子（LangGraph 已成熟），从**协作模式层**和**领域知识层**两端提升生成质量与可维护性。
> 依据：当前版本已实跑验证（生成 LoginPage.kt 5744 chars、12 测试通过）；已确认官方知识源 `Tencent-TDS/KuiklyUI-AI` 存在且自带更新机制。
> 范围：本文只规划"借什么 + 怎么落到代码"，不含实现。

## 0. 总览（三句话）

1. **编排层不动**：LangGraph 是成熟框架，6 节点拓扑（线性 / Fan-out / Fan-in / 条件循环）合理，保留。
2. **领域知识层是最大短板也是最大机会**：手写 5 个 `examples/*.kt` + `prompts.py` 里静态 `KUIKLY_API_REFERENCE` 表 → 换成官方 `Tencent-TDS/KuiklyUI-AI` 的规则/Skills（动态、权威、永远最新）。
3. **协作模式层做"加固"**：补 Supervisor 路由、强类型 State、Human-in-the-loop，让多 Agent 更稳、更可控。

## 1. 分层诊断

| 层 | 现状 | 评估 | 改进方向 |
|----|------|------|---------|
| 编排层 | LangGraph 6 节点 | ✅ 成熟 | 不动 |
| 领域知识层 | 5 手写 .kt + 静态 API 表 | ⚠️ 过时/不全 | 借官方 KuiklyUI-AI → RAG |
| 协作模式层 | 线性+并行+汇总+循环 | 🟡 可用 | 补 Supervisor / 强类型 / HITL |
| 校验层 | 括号匹配 + LLM 自查 | ⚠️ 非真编译 | kotlinc（见 Phase 3） |

---

## Phase 1 · 领域知识层（优先级最高，ROI 最大）

### 1.1 轻量 RAG：借官方 `KuiklyUI-AI` 规则

- **来源仓库**：`github.com/Tencent-TDS/KuiklyUI-AI`（腾讯官方 Kuikly AI 开发规则/Skills 库）
- **它提供什么**：
  - `kuiklyDSL.mdc`：Kuikly DSL 跨端开发规范（架构/编码/UI/性能/状态管理）
  - `kuiklyComposeDSL.mdc`：Compose DSL 规范
  - Skills：`kuikly-ui-framework`、`kuikly-animation`、`kuikly-network-and-json` 等（按场景拆分的知识包）
  - **自动更新机制**：每次调用先 clone / 拉最新 `Tencent-TDS/KuiklyUI` 源码（7 天周期），知识永远最新
- **做法**：把对应 rule 文本当"检索源"，节点生成前按页面类型注入 prompt，取代 `prompts.py` 里写死的 `KUIKLY_API_REFERENCE`。
- **落到代码**：
  - 新增 `retriever.py`：`load_kuikly_rules(dsl_type: str) -> str`，读本地 `KuiklyUI-AI/rules/*.mdc`
  - `prompts.py`：把常量 `KUIKLY_API_REFERENCE` 改为调用 `load_kuikly_rules()` 的动态字符串
- **成本**：低（clone 一次仓库，本地读文本）。质量立刻 > 手写静态表。

### 1.2 标准 RAG：向量库检索

- 把 Kuikly 官方文档/源码切块 → Chroma 向量库（现成模板：`ankitprasad81/langgraph` 的 `5_rag_agent.py`）
- 每个节点 `fill_prompt` 前 `retrieve(query=页面类型, top_k=3)`，注入最相关片段
- **落到代码**：
  - `retriever.py` 增加 `embed_and_store()` + `retrieve()`
  - `nodes.py` 各 `node_*` 在构造 prompt 处加检索调用
- **成本**：中（装 `chromadb`、建索引）

---

## Phase 2 · 协作模式层

### 2.1 加 Supervisor 路由节点

- **借什么**：社区公认的 Planner-Architect-Coder / Supervisor 模式（参考 `Tammyb2/agentic-coder-pipeline`）
- **做什么**：在 ① 前加一个 `supervisor`，按需求复杂度决定：简单页直走 ②；复杂页先拆更细子任务再分发
- **落到代码**：`graph.py` 加 `add_node("supervisor", ...)` + 条件边 `START → supervisor → {parse_requirement | decompose_page}`
- **收益**：避免复杂需求一次生成质量崩

### 2.2 State 强类型化（Pydantic）

- **借什么**：成熟项目的做法——用 Pydantic 模型定义 state，agent 交接时自动校验字段（`Tammyb2` 用 Pydantic 做 hand-off 校验）
- **落到代码**：`state.py` 的 `GraphState` 从裸 `TypedDict` 升级为 Pydantic model，或用 `pydantic.ValidationError` 兜底节点返回值
- **收益**：节点间传参出错早暴露，而非跑到后面才崩

### 2.3 Human-in-the-loop（人工确认）

- **借什么**：LangGraph 官方能力——state checkpoint，可在指定节点暂停等人确认
- **落到代码**：`compile_check` 后加 `interrupt_before=["auto_fix"]`，只有人点"继续"才进入修正
- **收益**：防止自动修正把代码改坏；适合生产环境

---

## Phase 3（可选）· 真编译硬校验

| 档位 | 做法 | 落到代码 | 成本 |
|------|------|---------|------|
| ① 最轻 | `kotlinc` 语法级校验 | 替换 `nodes.py:_rule_check` 的括号匹配 | 低（本地可跑） |
| ② 中等 | `kotlinc` + KuiklyUI `core`/`core-annotations` 作 classpath，验 `@Page`/`Pager()`/import 真实存在 | `_rule_check` 调外部 `kotlinc` | 中 |
| ③ 最重 | clone `Tencent-TDS/KuiklyUI` 跑 Gradle `compileKotlin` | CI 流水线调用 | 高（需 JDK17 + Android Studio/DevEco） |

> 建议：本地只做 ①；② 可作为可选增强；③ 只在 CI 跑，别本地强制。

---

## 2. 一周可落地清单（建议顺序）

1. **Day1-2**：Phase 1.1 借官方 rules（最低成本、最大收益）
2. **Day3**：Phase 3 ① `kotlinc` 语法校验（替换假检查）
3. **Day4-5**：Phase 2.2 强类型 State
4. **Day5+**：Phase 1.2 标准 RAG / Phase 2.1 Supervisor / Phase 2.3 HITL（按需）

## 3. 风险

- 官方 rules 体积大，**全量注入会爆 token** → 必须做检索 / top-k，绝不全文硬塞。
- RAG 索引质量依赖切片策略，先小范围验证再扩大。
- 真编译工具链重，只在 CI 跑；本地强制会拖慢开发。
- 借社区 demo 代码需谨慎：多为 0-star 教学级，借"模式"不借"实现"。

---

## 4. 进度追踪（已落地）

| 阶段 | 状态 | 关键改动 | 验证 |
|------|------|---------|------|
| Phase 1.1 轻量 RAG（官方 rules） | ✅ 已完成 | 新增 `retriever.py` 读 `KuiklyUI-AI/rules/*.mdc`；`prompts.py` 动态注入，缺仓库降级静态表 | 实跑生成 SettingsPage.kt（6362 chars） |
| Phase 3 ① kotlinc 语法校验 | ✅ 已完成 | `nodes.py:_rule_check` 优先调 kotlinc；过滤 classpath 噪声（unresolved reference / cannot infer type 等） | 坏代码捕获 `Missing '}'`；LoginPage 不再误报；13→15 测试全绿 |
| Phase 2.2 强类型 State（Pydantic） | ✅ 已完成 | `state.py:GraphState` 升级为 Pydantic `BaseModel`（extra="allow"）；节点内用 `_state_values()` shim 把模型转回 dict | 实跑 6 节点全流程 + auto_fix 循环，生成 NewsListPage.kt；15 测试全绿 |

> **Phase 2.2 踩坑记录（给 nika 的学习点）**：LangGraph 在 Pydantic state 模式下，会把 state 当作 **`GraphState` 模型实例**传给节点函数，而不是 dict。所以节点里原来的 `state.get("xxx")` 会直接 `AttributeError`。
> 解决：保留 Pydantic 作为"单一事实来源"（LangGraph 在节点**写回**时做真类型校验，传参出错早暴露），再在每个节点开头用 `_state_values(state)` 把模型转回 dict，兼容 `state.get()` 习惯。这是"强类型校验"和"不破坏现有节点代码"两全的写法。

### 下一步可选（按路线图顺序）
- Phase 1.2 标准 RAG（Chroma 向量库，需装 chromadb + 嵌入模型）
- Phase 2.1 Supervisor 路由节点
- Phase 2.3 Human-in-the-loop（interrupt_before auto_fix）
- Phase 3 ②/③（kotlinc + Kuikly classpath / Gradle CI，建议只在 CI 跑）
