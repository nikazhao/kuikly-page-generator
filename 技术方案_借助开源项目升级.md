# 技术方案_Kuikly页面生成器借助开源项目升级

> 类型：技术方案类（动手前对齐）
> 作者：nikazhao
> 日期：2026-08-24
> 关联：Graph/优化路线图.md、Graph/IMPLEMENTATION_SUMMARY.md

---

## 精简版

当前 Kuikly 页面生成器（LangGraph 单 Agent 图流水线）已具备 6 节点编排、Pydantic 强类型 State、kotlinc 真实语法校验、官方规则检索、--trace 可观测、15 测试全绿。本方案目标：**借助成熟开源项目把项目从「能跑的 CLI 实验」升级为「团队可直接使用、效果可量化、演示有说服力」的工具**，全程不重造 LangGraph 编排轮子。首期落地 **Web UI（让工具被人用）+ 量化 eval（让效果有数字）** 两项，借力 `yigit353/LangGraph-FastAPI-Streamlit` 与官方 `Tencent-TDS/KuiklyUI-AI` 知识体系；后续扩展官方 Kuikly MCP 接入与生成器 MCP Server 暴露。

---

## 一、背景与目标

### 1.1 已具备基础（现状盘点）

| 能力 | 状态 | 说明 |
|------|------|------|
| 6 节点图编排 | ✅ | 解析→拆解→并行生成→组装→检查→修正，覆盖线性/并行/汇总/循环 |
| State 强类型 | ✅ | Pydantic `BaseModel` + `_state_values()` shim（Phase 2.2） |
| 真实语法校验 | ✅ | kotlinc 调用 + classpath 噪声过滤（Phase 3 ①） |
| 官方知识检索 | ✅ | `retriever.py` 读本地克隆的 `KuiklyUI-AI/rules/*.mdc`（Phase 1.1） |
| 可观测 | ✅ | `--trace` 实时打印每步 state 快照 |
| 测试 | ✅ | 15/15 单元测试通过 |
| 入口 | ⚠️ 仅 CLI | `python -m src.main "..."`，无图形界面、无他人可调用接口 |

### 1.2 目标

1. **可用性**：从命令行升级为网页工具，团队成员可自然语言输入、直接拿到 .kt 并下载。
2. **可量化**：建立 eval 基线，量化「生成通过率 / 自动修正次数 / 平均耗时」，作为效果对比标尺。
3. **可演示**：步骤时间线可视化，演示时能讲清「Graph 每一步在做什么」。
4. **可集成**（后续）：接入官方 Kuikly MCP 拿实时知识；把生成器暴露为 MCP Server 让其他 AI 工具可调用。
5. **借力不造轮子**：编排层 LangGraph 已经成熟，专注「领域知识层 + 可用性层 + 度量层」三个短板。

---

## 二、借力清单（真实开源项目）

| 借力对象 | 来源 | 借什么 | 用在哪 | 授权 |
|---------|------|--------|--------|------|
| **Kuikly 官方 AI 体系** | `github.com/Tencent-TDS/KuiklyUI-AI` | Rules / Skills / 知识库（已克隆本地）；MCP 协议思路 | 阶段A 知识源、阶段C 协议参考 | 开源（腾讯） |
| **LangGraph + FastAPI + Streamlit** | `github.com/yigit353/LangGraph-FastAPI-Streamlit` | 分层架构：LangGraph 后端 / FastAPI 中转 / Streamlit 前端，SSE 流式推 state | 阶段B Web UI 架构 | MIT |
| **Langgraph Streamlit Chat Interface** | `github.com/aminghrz/Langgraph-Streamlit-Chat-Interface` | 轻量单文件 Streamlit UI 模板，可_host 任意 LangGraph | 阶段B 快速起步模板 | MIT |
| **langgraph-code-agent** | `github.com/mayankysharma/langgraph-code-agent` | 生成后自动 eval 的思路（静态分析 + 输出相关性评估） | 阶段D eval 设计参考 | MIT |
| **MCP Python SDK** | `github.com/modelcontextprotocol/python-sdk` | 把 `generate_page` 包成标准 MCP tool | 阶段C MCP Server | MIT |

> 原则：借「模式与架构」不借「玩具实现」。GitHub 上多数 LangGraph 代码生成 demo 为 0-star 教学级，仅作模式参考；核心知识源只信官方 `Tencent-TDS/KuiklyUI-AI`。

---

## 三、分阶段方案

### 阶段A · 知识源升级（接官方 Kuikly AI 体系）

**方案**
- 确认官方 Kuikly MCP 是否已公开发布（官方路线图标注为「陆续发布」）。
- 若已发布：把 `retriever.py` 的知识来源从「读本地 .mdc 文件」升级为「运行时调官方 Kuikly MCP Server」，获取实时、不过期的组件 API / 文档索引。
- 若未发布：保持本地克隆 Rules（现状），但把 `retriever.py` 抽象为一层统一接口 `KuiklyKnowledgeSource`，为阶段C 自建官方知识 MCP 包埋点。

**借力**：`Tencent-TDS/KuiklyUI-AI`、官方 Kuikly MCP 协议。
**落地文件**：`src/retriever.py`（抽象接口化）、`src/mcp_kuikly.py`（MCP 客户端，条件实现）。
**验收标准**：
- 生成质量不退化（输出 .kt 结构正确、可过 kotlinc 校验）；
- 若接 MCP：能拉到与本地 Rules 一致的官方组件 API，且版本为最新；
- 接口层对上层节点透明，节点代码零改动。

---

### 阶段B · Web UI（首期重点）

**方案**
- 用 Streamlit 包一层网页：左侧自然语言输入框 + 生成按钮；右侧分步时间线（复用 `--trace` 的 state 快照逻辑）+ 最终 .kt 代码块 + 下载按钮。
- 架构分两层（借 `yigit353` 的分层思路）：
  - 后端：`src/graph.py` 的 `get_graph()` 不变，新增 `src/run_pipeline.py` 暴露 `stream_steps(requirement)` 生成器，逐步 yield `(step, state)`。
  - 前端：`webui/app.py` 用 Streamlit 消费该生成器，逐步刷新步骤卡片。
- 首期单文件 Streamlit 即可（借 `aminghrz` 轻量模板），跑通后再按需拆 FastAPI。

**借力**：`yigit353/LangGraph-FastAPI-Streamlit`、`aminghrz/Langgraph-Streamlit-Chat-Interface`。
**落地文件**：`src/run_pipeline.py`（新增，封装 stream）、`webui/app.py`（新增）、`requirements.txt`（加 streamlit）。
**验收标准**：
- `streamlit run webui/app.py` 启动，浏览器打开 `localhost:8501`；
- 输入「一个登录页面」→ 右侧出现 6 步时间线（解析/拆解/并行生成/组装/检查/修正）→ 最终 .kt 显示在代码块并可下载；
- 生成失败（success=False）时有明确错误提示，不崩溃；
- 单页生成耗时被展示。

---

### 阶段C · 暴露为 MCP Server（让别人的 AI 能调用）

**方案**
- 用 MCP Python SDK 把 `generate_page` 包成标准 MCP tool：`generate_kuikly_page(requirement: str) -> str`，返回 .kt 代码或错误信息。
- 配置示例写入 `mcp_server.json`，使 CodeBuddy / Cursor 等支持 MCP 的客户端可直接调用。

**借力**：MCP Python SDK、官方 Kuikly MCP 协议示范。
**落地文件**：`mcp_server.py`（新增）、`README.md`（加 MCP 接入说明）。
**验收标准**：
- 启动 MCP Server 后，在支持 MCP 的客户端配置并能列出 `generate_kuikly_page` 工具；
- 客户端发「生成一个设置页面」→ 返回合法 .kt 文本；
- Server 异常不导致客户端崩溃（返回结构化错误）。

---

### 阶段D · 量化 eval（效果标尺）

**方案**
- 新增 `eval/run_eval.py`：加载 `tests/test_cases.json`（10 个用例），逐条调用 `generate_page`，收集 `success / fix_attempts / elapsed_seconds / error_count`。
- 输出 `eval/report.md`：通过率、平均修正次数、平均耗时、各用例明细。
- 阶段A 接入 MCP 后，跑「接入前 / 接入后」两次对比，形成量化收益数字。

**借力**：`mayankysharma/langgraph-code-agent` 的「生成后自动评估」思路（本项目用自研统计，不引入 Judgeval 重依赖）。
**落地文件**：`eval/run_eval.py`（新增）、`eval/report.md`（生成）。
**验收标准**：
- 一键跑完 10 个用例并产出报告；
- 报告含通过率等核心指标，可作为效果对比基线；
- 阶段A 前后对比数字可被引用（如「通过率 X% → Y%」）。

---

## 四、推荐首期范围

**首期做 阶段B（Web UI）+ 阶段D（eval）**，理由：
1. 两者都不动现有 6 节点编排层，风险最低；
2. Web UI 直接把「CLI 实验」变成「可演示、可给人用的工具」，可用性跃升；
3. eval 给出数字标尺，为后续阶段A 接入 MCP 的「效果提升」提供对比依据；
4. 两项均可在本地独立完成，不依赖官方 MCP 发布节奏。

阶段A / 阶段C 作为二期，待官方 MCP 发布或自评需求明确后再推进。

---

## 五、风险与对策

| 风险 | 对策 |
|------|------|
| 官方 Kuikly MCP 尚未公开发布 | 阶段A 先保持本地 Rules；接口抽象化，发布后无缝切换 |
| Streamlit 与现有 stdout 打印冲突 | Web UI 走独立 `run_pipeline.py` 生成器，不复用 `main.py` 的 print 逻辑 |
| Web UI 长文本（.kt）刷屏卡顿 | 代码块用 Streamlit `st.code` 渲染 + 下载按钮，步骤卡片截断长字段（复用 `--trace` 截断逻辑） |
| eval 用例覆盖不足导致数字不具代表性 | 先以现有 10 用例为基线，后续按需扩充 `test_cases.json` |
| 借社区 demo 代码引入不必要依赖 | 仅借架构模式，核心逻辑基于自有 `graph.py` / `nodes.py` 实现 |

---

## 六、验收总表

| 阶段 | 关键交付 | 一眼验收 |
|------|---------|---------|
| A 知识源 | 统一知识接口 +（条件）MCP 客户端 | 生成质量不退化，知识可热更新 |
| B Web UI | `webui/app.py` 可启动、可生成、可下载 | 浏览器输需求→出 .kt |
| C MCP Server | `mcp_server.py` 可被客户端调用 | 客户端列工具并成功生成 |
| D eval | `eval/report.md` 通过率等数字 | 一键产出前后对比 |

---

## 七、下一步

待本方案确认后，按「阶段B → 阶段D → 阶段A → 阶段C」顺序落地；每阶段完成即跑对应验收标准，全部通过后再进入下一阶段。
